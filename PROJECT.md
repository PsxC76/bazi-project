# 八字命理案例库 - 项目设计文档

> 版本：v1.0 | 最后更新：2026-09-08

---

## 一、项目概述

### 1.1 项目定位
面向公众的八字命理案例管理平台，支持八字排盘、案例存储、条件筛选等功能。

### 1.2 技术栈

| 层面 | 技术 | 说明 |
|------|------|------|
| 前端 | Vue 3 + Nuxt 3 + Tailwind CSS | SSR支持SEO |
| 后端 | Python + FastAPI | 高性能异步 |
| 数据库 | PostgreSQL | JSON支持好，全文搜索强 |
| 缓存 | Redis | Session、热点数据 |
| 部署 | Docker + Nginx | 一键部署 |

### 1.3 配色方案

| 用途 | 颜色 | 色值 |
|------|------|------|
| 主色 | 蓝色 | `#2563eb` |
| 主色-浅 | 浅蓝 | `#3b82f6` |
| 主色-深 | 深蓝 | `#1d4ed8` |
| 强调色 | 天蓝 | `#0ea5e9` |
| 背景色 | 浅灰蓝 | `#f8fafc` |
| 文字色 | 深灰 | `#1a1a2e` |
| 次要文字 | 中灰 | `#64748b` |

---

## 二、用户系统

### 2.1 用户角色

| 角色 | 权限 |
|------|------|
| 游客 | 浏览首页、查看公开案例 |
| 普通用户 | 注册、登录、排盘、管理自己的案例、绑定邮箱、评价他人发布的公开案例 |
| 管理员 | 所有普通用户权限 + 管理所有用户 + 管理所有案例 +管理所有的评论|

### 2.2 注册

| 字段 | 规则 |
|------|------|
| 账号 | 必填，不少于6位，仅字母/数字/下划线 |
| 密码 | 必填，不少于6位，仅字母/数字/下划线 |
| 确认密码 | 必填，必须与密码一致 |
| 邮箱 | 必填，输入的验证码必须与服务器发送的验证码一致 |

注册成功后：
- 自动生成 UID（格式：`UID_xxxxxxxx`，8位随机字符串）
- 自动登录，跳转至首页页面

### 2.3 登录

| 字段 | 规则 |
|------|------|
| 账号 | 必填 |
| 密码 | 必填 |

登录成功后跳转至案例库页面。

### 2.4 个人中心

| 功能 | 说明 |
|------|------|
| 头像上传 | 支持 JPG/PNG/GIF/WebP，最大2MB |
| 邮箱绑定 | 发送6位验证码 → 输入验证 → 绑定成功 |
| 邮箱解绑 | 发送6位验证码 → 输入验证 → 解绑成功 |
| 修改昵称 | 选填 |
| 个人简介 | 选填 |
| 修改密码 | 需输入原密码 |

### 2.5 路由守卫

以下页面需要登录才能访问：
- `/cases/new`（新建案例）
- `/cases/[id]/edit`（编辑案例）
- `/profile`（个人中心）

未登录时跳转至 `/login`。

---

## 三、八字排盘功能

### 3.1 输入字段

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| 性别 | 单选 | 是 | 男/女 |
| 出生年 | 数字 | 是 | 1900-2100 |
| 出生月 | 下拉 | 是 | 1-12 |
| 出生日 | 下拉 | 是 | 1-31 |
| 出生时 | 下拉 | 否 | 12时辰，未知则不选 |

### 3.2 计算结果

| 内容 | 说明 |
|------|------|
| 四柱八字 | 年柱、月柱、日柱、时柱（天干+地支） |
| 十神 | 每柱对应十神（比肩/劫财/食神/伤官/偏财/正财/七杀/正官/偏印/正印） |
| 日主 | 日柱天干及其五行属性 |
| 五行统计 | 金/木/水/火/土各多少个 |
| 地支藏干 | 每个地支中暗藏的天干 |
| 大运 | 12步大运（每步10年），显示干支和十神 |

### 3.3 排盘规则

基于《八字与用神》（朱祖夏）的排盘规则：
- **年柱**：以立春为界，立春前算前一年
- **月柱**：以节气为界，用"甲己之年丙作首"口诀推月干
- **日柱**：公式法计算日干支
- **时柱**：用"甲己还生甲"口诀推时干
- **大运**：阳男阴女顺排，阴男阳女逆排

---

## 四、案例管理功能

### 4.1 案例字段

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| 姓名 | 文本 | 是 | 命主姓名 |
| 性别 | 单选 | 是 | 男/女 |
| 出生地点 | 文本 | 否 | |
| 出生年 | 数字 | 是 | |
| 出生月 | 数字 | 是 | |
| 出生日 | 数字 | 是 | |
| 出生时 | 数字 | 否 | |
| 是否公开 | 开关 | 否 | 默认否 |
| 断语 | 长文本 | 否 | 对该命例的判断 |
| 备注 | 长文本 | 否 | 其他信息 |
| 标签 | 键值对列表 | 否 | 见下方标签系统 |

### 4.2 标签系统

标签为键值对结构，格式：`标签名: 标签值`

**固定标签**（系统预置）：
- 性别（已作为独立字段）

**常用标签名**（用户可自定义新的）：
- 职业（如：教师、医生、工程师）
- 学历（如：本科、硕士、博士）
- 婚姻（如：已婚、未婚、离异）
- 财富（如：富裕、中等、贫困）
- 寿夭（如：长寿、中寿、短寿）
- 健康（如：良好、一般、体弱）
- 性格（如：外向、内向、稳重）
- 其他

用户在创建/编辑案例时可添加多个标签，每个标签包含：
- 标签名（下拉选择已有名称，或手动输入新名称）
- 标签值（手动输入）

### 4.3 案例列表

| 功能 | 说明 |
|------|------|
| 分页 | 每页20条 |
| 搜索 | 按姓名或断语关键词搜索 |
| 筛选 | 按性别、标签名+标签值筛选 |
| 排序 | 按创建时间倒序 |

### 4.4 案例可见性

| 场景 | 可见范围 |
|------|----------|
| 我的案例 | 自己创建的所有案例 |
| 公开案例 | 所有用户标记为"公开"的案例 |
| 案例详情 | 自己的案例 + 公开案例 |

### 4.5 案例操作

| 操作 | 权限 |
|------|------|
| 创建 | 登录用户 |
| 查看 | 案例所有者 + 公开案例所有人 |
| 编辑 | 案例所有者 + 管理员 |
| 删除 | 案例所有者 + 管理员 |
| 重新计算八字 | 案例所有者 |

---

## 五、页面设计

### 5.1 页面清单

| 路径 | 页面 | 需登录 |
|------|------|--------|
| `/` | 首页 | 否 |
| `/bazi` | 八字排盘 | 否（但保存案例需登录） |
| `/cases` | 案例库（我的） | 是 |
| `/cases/new` | 新建案例 | 是 |
| `/cases/[id]` | 案例详情 | 是（但公开案例不需要登录） |
| `/cases/[id]/edit` | 编辑案例 | 是 |
| `/public` | 公开案例 | 否 |
| `/login` | 登录 | 否 |
| `/register` | 注册 | 否 |
| `/profile` | 个人中心 | 是 |

### 5.2 全局布局

**顶部导航栏**：
- 左侧：Logo（含有八字命理元素的二次元卡通图标 + "八字命理案例库"）
- 中间：导航菜单（首页、排盘、案例库、公开案例）
- 右侧：
  - 未登录：登录/注册按钮
  - 已登录：新建案例按钮 + 用户头像/昵称

**用户头像区域交互**：
- 点击头像/昵称 → 跳转个人中心
- 鼠标悬停1秒 → 显示下拉菜单（个人中心、我的案例、退出登录）
- 鼠标进入下拉菜单 → 可以点击个人中心、我的案例、退出登录，而不是鼠标离开用户头像/昵称区域后下拉菜单就直接隐藏了
- 鼠标离开下拉菜单 → 隐藏下拉菜单

**页脚**：版权信息

### 5.3 首页

- Hero区域：标题 + 简介 + CTA按钮（开始排盘、案例库）
- 功能介绍：3个功能卡片（智能排盘、案例管理、条件筛选）
- CTA区域：注册引导

### 5.4 排盘页面

- 输入表单：性别、年月日时
- 排盘结果：
  - 四柱八字（4个卡片，日柱高亮）
  - 五行统计（柱状图或数字）
  - 地支藏干
  - 大运列表
- 底部按钮：保存为案例

### 5.5 案例列表页面

- 顶部筛选栏：关键词搜索、性别筛选、标签筛选
- 案例卡片列表：姓名、性别、出生日期、八字预览、断语预览、标签
- 分页

### 5.6 案例详情页面

- 基本信息：姓名、性别、出生信息
- 四柱八字展示
- 五行统计
- 大运列表
- 断语与备注
- 标签
- 操作按钮：编辑、删除

### 5.7 个人中心页面

- 头像上传
- 邮箱绑定
- 基本信息编辑
- 修改密码
- 退出登录

---

## 六、API设计

### 6.1 用户相关

| 方法 | 路径 | 说明 | 需登录 |
|------|------|------|--------|
| POST | `/api/v1/users/register` | 注册 | 否 |
| POST | `/api/v1/users/login` | 登录 | 否 |
| GET | `/api/v1/users/me` | 获取当前用户信息 | 是 |
| PUT | `/api/v1/users/me` | 更新用户信息 | 是 |
| POST | `/api/v1/users/me/password` | 修改密码 | 是 |
| POST | `/api/v1/users/me/avatar` | 上传头像 | 是 |
| POST | `/api/v1/users/me/email/send-code` | 发送邮箱验证码 | 是 |
| POST | `/api/v1/users/me/email/verify` | 验证邮箱 | 是 |
| GET | `/api/v1/users/list` | 用户列表（管理员） | 是 |

### 6.2 案例相关

| 方法 | 路径 | 说明 | 需登录 |
|------|------|------|--------|
| POST | `/api/v1/cases` | 创建案例 | 是 |
| GET | `/api/v1/cases` | 案例列表（我的+公开） | 是 |
| GET | `/api/v1/cases/{id}` | 案例详情 | 是(但查看公开案例不需要登录) |
| PUT | `/api/v1/cases/{id}` | 更新案例 | 是 |
| DELETE | `/api/v1/cases/{id}` | 删除案例 | 是 |
| POST | `/api/v1/cases/{id}/recalculate` | 重新计算八字 | 是 |
| GET | `/api/v1/cases/tags/list` | 标签列表 | 是 |
| GET | `/api/v1/cases/tags/names` | 标签名列表 | 是 |

### 6.3 排盘相关

| 方法 | 路径 | 说明 | 需登录 |
|------|------|------|--------|
| POST | `/api/v1/bazi/calculate` | 八字排盘计算 | 否 |

### 6.4 通用响应格式

**成功**：
```json
{
  "id": 1,
  "name": "张三",
  ...
}
```

**列表**：
```json
{
  "total": 100,
  "page": 1,
  "page_size": 20,
  "items": [...]
}
```

**错误**：
```json
{
  "detail": "错误信息"
}
```

或校验错误：
```json
{
  "detail": [
    {"loc": ["body", "username"], "msg": "账号只能包含字母、数字、下划线", "type": "value_error"}
  ]
}
```

---

## 七、数据库设计

### 7.1 用户表 (users)

| 字段 | 类型 | 说明 |
|------|------|------|
| id | INTEGER PK | 自增主键 |
| username | VARCHAR(50) UNIQUE | 账号 |
| email | VARCHAR(255) UNIQUE | 邮箱（可空） |
| email_verified | BOOLEAN | 邮箱是否已验证 |
| hashed_password | VARCHAR(255) | 密码哈希 |
| nickname | VARCHAR(100) | 昵称/UID |
| avatar | VARCHAR(500) | 头像路径 |
| bio | TEXT | 个人简介 |
| is_active | BOOLEAN | 是否启用 |
| is_admin | BOOLEAN | 是否管理员 |
| created_at | TIMESTAMP | 创建时间 |
| updated_at | TIMESTAMP | 更新时间 |

### 7.2 案例表 (cases)

| 字段 | 类型 | 说明 |
|------|------|------|
| id | INTEGER PK | 自增主键 |
| user_id | INTEGER FK | 关联用户 |
| name | VARCHAR(100) | 姓名 |
| gender | VARCHAR(10) | 性别 |
| birth_place | VARCHAR(200) | 出生地点 |
| birth_year | INTEGER | 出生年 |
| birth_month | INTEGER | 出生月 |
| birth_day | INTEGER | 出生日 |
| birth_hour | INTEGER | 出生时（可空） |
| birth_minute | INTEGER | 出生分（可空） |
| is_lunar | BOOLEAN | 是否农历 |
| bazi_result | JSON | 八字计算结果 |
| verdict | TEXT | 断语 |
| notes | TEXT | 备注 |
| is_public | BOOLEAN | 是否公开 |
| created_at | TIMESTAMP | 创建时间 |
| updated_at | TIMESTAMP | 更新时间 |

### 7.3 标签表 (tags)

| 字段 | 类型 | 说明 |
|------|------|------|
| id | INTEGER PK | 自增主键 |
| name | VARCHAR(50) | 标签名（如"职业"） |
| value | VARCHAR(200) | 标签值（如"教师"） |
| user_id | INTEGER FK | 创建者（可空=系统标签） |
| created_at | TIMESTAMP | 创建时间 |

### 7.4 案例-标签关联表 (case_tags)

| 字段 | 类型 | 说明 |
|------|------|------|
| case_id | INTEGER FK | 案例ID |
| tag_id | INTEGER FK | 标签ID |

### 7.5 邮箱验证码表 (email_verifications)

| 字段 | 类型 | 说明 |
|------|------|------|
| id | INTEGER PK | 自增主键 |
| email | VARCHAR(255) | 邮箱地址 |
| code | VARCHAR(6) | 验证码 |
| purpose | VARCHAR(20) | 用途（bind/reset） |
| is_used | BOOLEAN | 是否已使用 |
| expires_at | TIMESTAMP | 过期时间 |
| created_at | TIMESTAMP | 创建时间 |

---

## 八、已知问题 & 待开发

### 8.1 已知问题

| # | 问题 | 优先级 |
|---|------|--------|
| 1 | 注册登录入口偶尔消失（可能与热重载有关） | 高 |
| 2 | 控制台报 `ERR_CONNECTION_RESET`（后端重启时短暂断连） | 低 |
| 3 | 邮箱绑定功能未完整测试 | 中 |

### 8.2 待开发功能

| # | 功能 | 优先级 | 说明 |
|---|------|--------|------|
| 1 | 暗色模式 | 低 | 需要设计暗色配色方案 |
| 2 | 数据导出 | 低 | 导出为PDF/Excel |
| 3 | 付费功能 | 低 | 需要企业资质接入支付 |
| 4 | 农历转换 | 中 | 输入支持公历/农历切换 |
| 5 | 流年展示 | 中 | 排盘结果中增加流年信息 |
| 6 | 案例收藏 | 低 | 收藏公开案例 |
| 7 | 案例分享 | 低 | 生成分享链接 |

---

## 九、开发规范

### 9.1 前端规范

- Vue组件使用 `<script setup>` 语法
- 非TypeScript文件不使用TS语法（如 `catch (e)` 而非 `catch (e: any)`）
- 使用 Tailwind CSS 类名，自定义样式写在 `main.css`
- API请求统一使用 `useApi()` 工具

### 9.2 后端规范

- API路由统一前缀 `/api/v1`
- 认证使用 Bearer Token（JWT）
- 错误返回格式：`{"detail": "错误信息"}`
- 数据库操作使用 SQLAlchemy 异步

### 9.3 提交规范

```
类型: 简短描述

类型包括：
- feat: 新功能
- fix: 修复
- ui: UI调整
- docs: 文档
- refactor: 重构
```

---

## 十、部署流程

### 10.1 本地开发

```bash
# 启动数据库
docker-compose up -d db redis

# 启动后端
cd backend
pip install -r requirements.txt
uvicorn app.main:app --reload --port 8000

# 启动前端
cd frontend
npm install
npm run dev
```

### 10.2 服务器部署

```bash
# Docker一键部署
docker-compose up -d
```

### 10.3 邮件配置

在 `backend/.env` 中配置：
```
SMTP_HOST=smtp.qq.com
SMTP_PORT=465
SMTP_USER=你的邮箱@qq.com
SMTP_PASSWORD=***
```

---

*文档结束。请审阅并提出修改意见，确认后按此文档进行开发。*
